# api-quick-schedule
Projeto back-end para otimizar o agendamento entre clientes e prestadores de serviço.

# Como instalar o projeto?

    python -m venv venv

    venv\Scripts\activate

    pip install -r requirements.txt

    python manage.py migrate *Antes de rodar, é necessário criar a DB.

    python manage.py runserver